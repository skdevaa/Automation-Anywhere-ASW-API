{
  "filter": {
    "operator": "{{ inputs.filterOperand }}",
    "operands": {{
      [
        inputs.statusFilter
          ? { operator: "eq",        value: inputs.statusFilter,  field: "status" }
          : null,
        inputs.titleFilter
          ? { operator: "substring", value: inputs.titleFilter,   field: "title" }
          : null
      ].filter(Boolean)   // removes nulls
    }}
  },
  "page": {
    "offset": 0,
    "length": 100000
  }
}
